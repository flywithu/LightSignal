# 모델 카드

LightSignal이 신호등 인식에 쓰는 온디바이스 검출 모델에 대한 설명입니다. 앱 사용법은 [README.md](README.md)를 보세요.

## 개요

| 항목 | 값 |
|---|---|
| 아키텍처 | YOLOv8n (Ultralytics) |
| 입력 | 640 × 640 × 3, float32, 0..1 정규화, letterbox 패딩(gray 114) |
| 출력 | `[1, 9, 8400]` float32 — `xywh` 4 + 클래스 5 |
| 좌표계 | **입력 픽셀 공간 (0..640)**, 정규화 좌표 아님 |
| 파라미터 | 3.01 M |
| 파일 | float16 가중치 + **float32 I/O** TFLite, 약 6.2 MB |
| 배포 | GitHub Releases → 앱 OTA |

### 클래스 (순서 고정)

```
0  Green        초록
1  Left         좌회전 화살표
2  Red          빨강
3  Speed_Sign   속도 표지판
4  Yellow       노랑
```

이 순서는 앱 디코더와 학습 라벨이 공유하는 규약입니다. 순서가 바뀌면 빨간불이 다른 상태로 조용히 뒤바뀝니다. `Speed_Sign`은 학습에는 쓰지만 신호 상태로는 표시하지 않습니다.

**박스 규약은 신호등 하우징 전체**입니다 — 켜진 램프 하나가 아니라 3~4개 램프와 그 사이 몸체를 모두 포함합니다. 가로형 신호등 기준 폭이 높이의 약 2.3배가 됩니다.

---

## 학습

| 항목 | 값 |
|---|---|
| 데이터셋 | [original-korean-traffic-light](https://universe.roboflow.com/min-yong-park/original-korean-traffic-light) (CC BY 4.0) + 사용자 신고 실패 사례 |
| 해상도 | 640 |
| 에폭 | 50 (patience 15 조기 종료) |
| 배치 | 32 |
| 하드웨어 | Colab T4 |

### 성능 (test 스플릿)

| 지표 | 값 |
|---|---|
| mAP50-95 | 0.7141 |
| mAP50 | 0.9778 |
| **Red recall** | **0.9434** |
| 추론 시간 (T4, 1장) | 4.24 ms |

클래스별 mAP50-95:

| Green | Left | Red | Speed_Sign | Yellow |
|---|---|---|---|---|
| 0.5849 | 0.7977 | 0.5751 | 0.8819 | 0.7308 |

Red recall을 따로 보는 이유는, 이 앱에서 가장 비싼 실패가 **빨간불을 놓치는 것**인데 종합 mAP로는 그게 가려지기 때문입니다.

---

## 한계 (알려진 것)

숫자만 보고 과신하지 않도록, 확인된 약점을 그대로 적습니다.

### 1. 박스가 좌우로 뒤집히는 경우가 있습니다

학습에 Ultralytics 기본 좌우반전 증강(`fliplr=0.5`)이 켜진 채로 돌아갔습니다. 한국 가로형 신호등은 **빨강이 항상 맨 왼쪽**이고 하우징이 오른쪽으로 뻗는데, 반전된 이미지는 현실에 없는 배치를 가르칩니다.

실제 측정에서 모델은 같은 신호등에 대해 정방향과 거울상 박스를 **둘 다** 내놓았고, 신호등에 따라 어느 쪽이 이길지가 달라졌습니다. 한 샘플에서 우측 신호등의 최고 확신도 박스는 정답과 IoU 0.32에 그쳤습니다(정답에 맞는 박스는 확신도 0.06으로 묻힘).

- **영향:** 신호 상태 판별(빨강/초록)은 정확합니다. 화면에 그려지는 **박스 위치**가 어긋납니다.
- **해결 예정:** `fliplr=0.0`으로 재학습. 좌회전 화살표를 뒤집으면 우회전이 되어 `Left` 라벨이 오염되는 문제도 함께 해결됩니다.

### 2. 시점 차이

데이터셋은 **차량 전방 블랙박스 시점**입니다. 보행자 시점이나 크게 기울어진 각도는 검증되지 않았습니다.

### 3. 악조건

비·눈·야간·역광, 고가도로 아래 반복 신호등에서 놓치는 사례가 확인되었습니다. 앱의 실패 신고 기능이 모으는 것이 주로 이런 장면입니다.

### 4. 거리

먼 신호등은 픽셀 수가 부족해 놓칩니다. 앱은 이를 완화하려고 분석 프레임을 1920×1080으로 올리고 640 타일로 잘라 훑습니다(SAHI 방식). 오프라인 평가에서 이 방식이 먼 신호등 recall을 대략 두 배로 올렸습니다.

---

## 앱에서의 사용

모델 단독 성능과 앱의 체감 성능은 다릅니다. 앱은 프레임마다 이렇게 돌립니다.

| 설정 | 값 |
|---|---|
| 분석 프레임 | 1920 × 1080 |
| 슬라이스 | 640 px, 20% 겹침 |
| 확신도 임계값 | 0.30 |
| NMS IoU | 0.45 |
| NMS IoS | 0.60 (크기가 크게 다른 중복 박스 병합용) |
| 워커 | 3개 (각자 인터프리터 보유) |
| 델리게이트 | GPU 우선, 실패 시 멀티스레드 CPU |

슬라이스를 모델 입력과 같은 640으로 맞춰 **리샘플링 없이 1:1**로 먹입니다. 확대는 모델이 아니라 프레임 해상도에서 나옵니다.

---

## 변환 파이프라인

```
best.pt  →  ONNX (opset 13, simplify)  →  onnx2tf  →  float16 가중치 TFLite
```

Ultralytics의 **네이티브 TFLite export를 쓰면 안 됩니다.** 좌표 규약이 달라서 앱 디코더가 박스를 엉뚱한 곳에 그립니다. 반드시 위 onnx2tf 경로를 거쳐야 합니다.

### 배포 전 필수 검증

`v20260726`이 이 검증을 통과하지 못한 채 배포되어 앱을 망가뜨린 적이 있습니다.

1. ONNX 출력 shape이 `[1, 9, 8400]`인가
2. TFLite 입출력 dtype이 **float32**인가 — onnx2tf 버전에 따라 `*_float16.tflite`가 진짜 float16 I/O로 나오는 경우가 있고, 그러면 TFLite가 로드조차 못 합니다 (`Node number 1 (CONV_2D) failed to prepare`)
3. 입력 shape `[1, 640, 640, 3]`, 출력 shape `[1, 9, 8400]`
4. 실제 로드 + 더미 추론 성공
5. 좌표가 픽셀 공간(0..640)으로 나오는가 — `xywh.max()`가 1보다 훨씬 크면 정상

float16 I/O로 나왔다면 `saved_model`에서 `target_spec.supported_types = [tf.float16]`으로 재변환하면 가중치만 float16이고 I/O는 float32가 됩니다.

---

## 릴리스 이력

| 버전 | 모델 | 상태 |
|---|---|---|
| `v20260727` | YOLOv8n @ 640 | 현재 배포본 |
| `v20260726` | YOLOv8s @ 640 | **결함** — float16 I/O로 export되어 TFLite가 로드 불가. 앱은 이 모델을 거부하고 내장 모델로 되돌아갑니다. |

버전은 날짜(YYYYMMDD) 정수이며 앱은 설치된 값보다 큰 경우에만 갱신합니다.
